{
  "name": "ecmaless",
  "private": true,
  "license": "MIT",
  "scripts": {
    "out": "lerna exec --bail=false -- npm out",
    "up": "lerna exec -- npm up",
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap --hoist",
    "setup": "npm i && npm run bootstrap",
    "publish": "lerna publish",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^2.3.1"
  }
}
